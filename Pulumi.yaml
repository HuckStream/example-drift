name: Example
runtime:
  name: nodejs
  options:
    packagemanager: npm
    # See https://github.com/TypeStrong/ts-node/issues/1007
    # nodeargs: "--loader ts-node/esm --no-warnings"
description: Example program with drift detection
config:
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
